{
    "name": "@scramjet/vdm31",
    "description": "....",
    "main": "./main.py",
    "engines": {
      "python3": "3.10.0"
    },
    "scripts": {
      "build": "mkdir -p dist/__pypackages__/ && mkdir -p dist/elfs && cp *.py package.json dist/ && cp elfs/* dist/elfs && pip3 install -t dist/__pypackages__/ -r requirements.txt",
      "clean": "rm -rf ./dist ./*.tar.gz",
      "pack": "si seq pack ./dist/"
    }
}
